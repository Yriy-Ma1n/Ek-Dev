import { CurrencySwitcherPipe } from './currency-switcher.pipe';

describe('CurrencySwitcherPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencySwitcherPipe();
    expect(pipe).toBeTruthy();
  });
});
